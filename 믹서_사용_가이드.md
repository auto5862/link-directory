# 믹서 사용 가이드

**예치 → 출금 (다른 주소로)**

---

## 1. 세 가지 방식

| 방식 | 설명 |
|------|------|
| **TokenMixer** | 토큰 1개 (USDT TeatherUSD decimals 18 전용) |
| **MultiTokenMixer** | 공식 USDT + USDT TeatherUSD 같이 (decimals 6 기준) |

---

## 2. TokenMixer (USDT TeatherUSD 전용)

### ⚠️ decimals 확인 필수

TronScan에서 `TGacvY1cTSLKgWEPxXqWt8FFAQ86vpzM1U` decimals 확인:
- **decimals 6** → FIXED_AMOUNT = `1000 * 10^6`, approve 금액 동일
- **decimals 18** → FIXED_AMOUNT = `1000 * 10^18`, approve 금액 동일

### 배포 방법

1. https://www.tronide.io → **Mainnet**
2. `TokenMixer.sol` 열기 → decimals에 맞게 FIXED_AMOUNT 확인 (현재 10^6)
3. **Compile** → **Deploy** → `_token`: `TGacvY1cTSLKgWEPxXqWt8FFAQ86vpzM1U`
4. TronLink **Confirm**

### 배포 완료 (기존 — decimals 18 기준으로 배포됨)

| 항목 | 주소 |
|------|------|
| **믹서 주소** | `TJRMpywqc8FH5ankFBgYjjmWW5modzqwbq` |
| _token | USDT TeatherUSD `TGacvY1cTSLKgWEPxXqWt8FFAQ86vpzM1U` |

※ 토큰이 decimals 6이면 위 믹서는 동작 안 함. 10^6으로 재배포 필요.

### 예치 (deposit)

1. **approve** (믹서 주소, decimals에 맞는 금액: 6이면 `1000 * 10^6`, 18이면 `1000 * 10^18`)
2. **deposit(비밀해시)** 호출 — 비밀해시 = keccak256(abi.encodePacked(비밀값))

### 출금 (withdraw)

1. **withdraw(비밀값, 수신주소)** 호출

---

## 3. MultiTokenMixer (공식 USDT + USDT TeatherUSD — decimals 6만 정상)

### 배포 완료

| 항목 | 주소 |
|------|------|
| **믹서 주소** | `TC812jp3UnGKgjY4aZCvjNWre7b3YqXJVB` |
| _token1 | 공식 USDT `TR7NHqjeKQxGTCi8q8ZY4pL8otSzgjLj6t` |
| _token2 | USDT TeatherUSD `TGacvY1cTSLKgWEPxXqWt8FFAQ86vpzM1U` |

### 예치 (deposit)

1. 토큰 **approve** (믹서 주소, `1000 * 10^6`) — 공식 USDT만 정상
2. **deposit(토큰주소, 비밀해시)** 호출
   - 비밀해시 = keccak256(abi.encodePacked(비밀값, 토큰주소))

### 출금 (withdraw)

1. **withdraw(토큰주소, 비밀값, 수신주소)** 호출

---

## 4. 고정 금액

| 믹서 | FIXED_AMOUNT |
|------|--------------|
| TokenMixer (USDT TeatherUSD, decimals 6) | 1000 * 10^6 |
| TokenMixer (USDT TeatherUSD, decimals 18) | 1000 * 10^18 |
| MultiTokenMixer (공식 USDT) | 1000 * 10^6 |

---

## 5. 파일

| 파일 | 설명 |
|------|------|
| **TokenMixer.sol** | 토큰 1개용 |
| **MultiTokenMixer.sol** | USDT + 우리 토큰 같이 |
